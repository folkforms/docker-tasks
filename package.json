{
  "name": "docker-tasks",
  "version": "0.1.37",
  "description": "Shelljs code that executes common Docker tasks.",
  "main": "docker-tasks.js",
  "bin": {
    "docker-tasks": "./cli.js"
  },
  "scripts": {
    "docker": "node cli.js",
    "prune": "yarn install --production --force",
    "unprune": "yarn install",
    "release": "npm whoami && yarn run prune && npm version patch && npm publish && yarn run unprune",
    "test": "jest"
  },
  "author": "folkforms",
  "repository": {
    "type": "git",
    "url": "https://github.com/folkforms/docker-tasks"
  },
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^9.1.0",
    "js-yaml": "^4.0.0",
    "shelljs": "^0.8.4"
  },
  "devDependencies": {
    "jest": "^27.0.4"
  }
}
