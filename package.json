{
  "name": "docker-tasks",
  "version": "0.1.25",
  "description": "Shelljs code that executes common Docker tasks.",
  "main": "index.js",
  "scripts": {
    "docker": "node docker-tasks.js",
    "release": "npm version patch && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "folkforms",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^9.1.0",
    "js-yaml": "^4.0.0",
    "shelljs": "^0.8.4"
  }
}
